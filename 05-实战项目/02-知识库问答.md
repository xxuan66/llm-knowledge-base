# 实战项目二：知识库问答系统（RAG）

> 构建基于检索增强生成（RAG）的智能问答系统，让 LLM 能够基于你的私有知识库回答问题。

这是最典型、也最容易被做浅的 LLM 项目之一。真正有价值的知识库问答系统，不是“检索到点内容再让模型说一遍”，而是要做到：

- 检索对
- 回答忠实
- 来源可追溯
- 用户能信任

## 项目目标

实现一个完整的 RAG 系统，能够：

1. **文档处理** — 支持 PDF、Markdown、Word 等格式
2. **向量存储** — 高效存储和检索文档向量
3. **语义检索** — 基于语义相似度找到相关内容
4. **智能问答** — 结合检索结果生成准确答案
5. **来源追踪** — 显示答案的引用来源

## 先明确这个项目的难点

大多数 RAG demo 之所以看起来能跑，但一上线就出问题，通常是因为忽略了：

- 文档清洗质量
- 分块策略
- 检索召回质量
- 重排
- 回答约束
- 引用展示

所以这一类项目真正的重点，不只是“接一个向量库”，而是把整条链路做稳。

## 系统架构

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   原始文档   │ →  │   文档解析   │ →  │   向量化    │
└─────────────┘     └─────────────┘     └─────────────┘
                                               ↓
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│    答案     │ ←  │   生成答案   │ ←  │   向量检索  │
└─────────────┘     └─────────────┘     └─────────────┘
      ↑                                        ↑
      │           ┌─────────────┐             │
      └───────────│    问题     │─────────────┘
                  └─────────────┘
```

## 技术栈

| 组件 | 推荐方案 | 备选方案 |
|------|---------|---------|
| 文档解析 | PyMuPDF / Unstructured | pdfplumber |
| 向量化 | OpenAI Embeddings | BGE / Sentence-Transformers |
| 向量数据库 | Chroma / Milvus | FAISS / Pinecone |
| LLM | GPT-4 / Qwen | 本地模型（Llama 3） |
| 框架 | LangChain / LlamaIndex | 自定义实现 |

## 做项目时的判断原则

### 1. 先解决检索，再优化生成

如果检索就错了，后面的 LLM 再强也只能把错误说得更流畅。

### 2. 先做可追溯，再追求自然表达

对知识库问答来说，用户往往更看重“依据是什么”，而不只是“回答是否像人写的”。

### 3. 先做小范围高质量，再追求全量覆盖

知识库系统一开始就接入大量杂乱文档，通常效果不会好。

## 核心实现

### 1. 文档处理

```python
from pathlib import Path
from PyPDF2 import PdfReader

def load_documents(doc_dir: str) -> list[dict]:
    """加载目录下的所有文档"""
    docs = []
    
    for file_path in Path(doc_dir).rglob("*"):
        if file_path.suffix == ".pdf":
            text = extract_pdf(file_path)
        elif file_path.suffix == ".md":
            text = file_path.read_text(encoding="utf-8")
        elif file_path.suffix == ".txt":
            text = file_path.read_text(encoding="utf-8")
        else:
            continue
        
        docs.append({
            "content": text,
            "source": str(file_path),
            "filename": file_path.name
        })
    
    return docs

def extract_pdf(file_path: Path) -> str:
    """提取 PDF 文本"""
    reader = PdfReader(str(file_path))
    text = ""
    for page in reader.pages:
        text += page.extract_text() + "\n"
    return text
```

### 2. 文本分块

```python
def chunk_text(text: str, chunk_size: int = 500, overlap: int = 50) -> list[str]:
    """将文本分块"""
    chunks = []
    start = 0
    
    while start < len(text):
        end = start + chunk_size
        chunk = text[start:end]
        
        # 在句号处截断，避免切断句子
        if end < len(text):
            last_period = chunk.rfind("。")
            if last_period > chunk_size * 0.5:
                chunk = chunk[:last_period + 1]
                end = start + last_period + 1
        
        chunks.append(chunk)
        start = end - overlap
    
    return chunks
```

### 3. 向量化与存储

```python
import chromadb
from openai import OpenAI

class VectorStore:
    def __init__(self, collection_name="knowledge_base"):
        self.client = chromadb.PersistentClient("./chroma_db")
        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            metadata={"hnsw:space": "cosine"}
        )
        self.openai = OpenAI()
    
    def embed(self, texts: list[str]) -> list[list[float]]:
        """生成文本向量"""
        response = self.openai.embeddings.create(
            model="text-embedding-3-small",
            input=texts
        )
        return [d.embedding for d in response.data]
    
    def add_documents(self, docs: list[dict]):
        """批量添加文档"""
        for doc in docs:
            chunks = chunk_text(doc["content"])
            embeddings = self.embed(chunks)
            
            ids = [f"{doc['source']}_{i}" for i in range(len(chunks))]
            metadatas = [
                {"source": doc["source"], "chunk": i}
                for i in range(len(chunks))
            ]
            
            self.collection.add(
                ids=ids,
                embeddings=embeddings,
                documents=chunks,
                metadatas=metadatas
            )
    
    def search(self, query: str, top_k: int = 5) -> list[dict]:
        """语义检索"""
        query_embedding = self.embed([query])[0]
        
        results = self.collection.query(
            query_embeddings=[query_embedding],
            n_results=top_k
        )
        
        return [
            {
                "content": doc,
                "source": meta["source"],
                "distance": dist
            }
            for doc, meta, dist in zip(
                results["documents"][0],
                results["metadatas"][0],
                results["distances"][0]
            )
        ]
```

### 4. RAG 问答链

```python
class RAGSystem:
    def __init__(self, vector_store: VectorStore, model: str = "gpt-4"):
        self.vs = vector_store
        self.client = OpenAI()
        self.model = model
    
    def ask(self, question: str, top_k: int = 3) -> dict:
        """RAG 问答"""
        # 1. 检索相关内容
        results = self.vs.search(question, top_k)
        context = "\n\n".join([r["content"] for r in results])
        
        # 2. 构建提示词
        prompt = f"""基于以下上下文回答问题。如果上下文中没有相关信息，请说明。

上下文：
{context}

问题：{question}

请提供准确、简洁的回答，并在最后列出引用来源。"""
        
        # 3. 生成答案
        response = self.client.chat.completions.create(
            model=self.model,
            messages=[
                {"role": "system", "content": "你是一个知识库问答助手，基于提供的上下文回答问题。"},
                {"role": "user", "content": prompt}
            ]
        )
        
        answer = response.choices[0].message.content
        
        return {
            "question": question,
            "answer": answer,
            "sources": [
                {"source": r["source"], "content_preview": r["content"][:100]}
                for r in results
            ]
        }
```

## 完整项目结构

```
rag-system/
├── src/
│   ├── __init__.py
│   ├── loader.py        # 文档加载
│   ├── chunker.py       # 文本分块
│   ├── embedder.py      # 向量化
│   ├── store.py         # 向量存储
│   ├── retriever.py     # 检索器
│   └── generator.py     # 生成器
├── data/                # 原始文档
├── chroma_db/           # 向量数据库
├── main.py              # CLI 入口
├── api.py               # FastAPI 服务
├── requirements.txt
└── README.md
```

## 高级优化

### 1. 混合检索（Hybrid Search）

结合语义检索和关键词检索：

```python
def hybrid_search(query: str, top_k: int = 5):
    # 语义检索
    semantic_results = vector_store.search(query, top_k)
    
    # 关键词检索（BM25）
    keyword_results = bm25_search(query, top_k)
    
    # 融合排序
    return rerank(semantic_results, keyword_results)
```

### 2. 重排序（Re-ranking）

使用专门的重排序模型提升检索质量：

```python
from sentence_transformers import CrossEncoder

reranker = CrossEncoder("cross-encoder/ms-marco-MiniLM-L-6-v2")

def rerank(query: str, candidates: list[dict]) -> list[dict]:
    pairs = [(query, c["content"]) for c in candidates]
    scores = reranker.predict(pairs)
    
    # 按分数重新排序
    for c, s in zip(candidates, scores):
        c["rerank_score"] = s
    
    return sorted(candidates, key=lambda x: x["rerank_score"], reverse=True)
```

### 3. 查询扩展

```python
def expand_query(query: str) -> str:
    """使用 LLM 扩展查询，提升召回率"""
    response = client.chat.completions.create(
        model="gpt-3.5-turbo",
        messages=[
            {"role": "user", "content": f"""请将以下问题扩展为3个相关的搜索查询：
原问题：{query}

请以 JSON 数组格式输出："""}]
    )
    # 解析返回的查询列表，分别检索后合并结果
```

## 评估指标

| 指标 | 说明 | 目标值 |
|------|------|-------|
| 召回率 | 检索到相关文档的比例 | > 85% |
| 精确率 | 检索结果中相关文档的比例 | > 70% |
| 答案准确率 | 答案与参考答案的匹配度 | > 80% |
| 响应时间 | 从问题到答案的时间 | < 3s |

## 常见问题

### Q: 检索不到相关内容怎么办？
- 检查文档是否正确加载和分块
- 尝试降低检索阈值
- 使用查询扩展技术
- 优化分块策略（大小、重叠）

### Q: 答案引用错误？
- 检查元数据是否正确存储
- 使用重排序提升相关性
- 在提示词中强调"基于上下文回答"

### Q: 如何处理多语言文档？
- 使用多语言 Embedding 模型（如 bge-m3）
- 或为不同语言维护不同的集合

## 常见场景配置

### 场景 1：企业内部知识库

```python
enterprise_config = {
    "name": "企业知识库 RAG",
    "documents": {
        "formats": [".pdf", ".docx", ".pptx", ".md"],
        "chunk_size": 512,
        "chunk_overlap": 64,
        "metadata_fields": ["department", "doc_type", "version"]
    },
    "retrieval": {
        "top_k": 5,
        "similarity_threshold": 0.75,
        "rerank": True
    },
    "generation": {
        "model": "gpt-4",
        "temperature": 0.3,
        "system_prompt": """你是企业知识库助手。请基于提供的文档内容回答问题。
        
规则：
1. 只回答文档中明确包含的内容
2. 如果不确定，说明"根据现有文档无法确定"
3. 引用具体的文档来源
4. 涉及机密信息时拒绝回答"""
    }
}
```

### 场景 2：技术文档问答

```python
tech_doc_config = {
    "name": "技术文档问答 RAG",
    "documents": {
        "formats": [".md", ".rst", ".txt", ".html"],
        "chunk_size": 384,
        "chunk_overlap": 48,
        "preserve_code_blocks": True  # 保留代码块完整性
    },
    "retrieval": {
        "top_k": 3,
        "similarity_threshold": 0.7,
        "code_boost": True  # 代码片段提升权重
    },
    "generation": {
        "model": "gpt-4",
        "temperature": 0.2,
        "system_prompt": """你是技术文档助手，帮助用户理解和使用 API/SDK。

回答要求：
1. 提供清晰的概念解释
2. 附带可运行的代码示例
3. 说明常见问题和注意事项
4. 引用文档的具体章节"""
    }
}
```

### 场景 3：客服知识库

```python
customer_service_config = {
    "name": "客服知识库 RAG",
    "documents": {
        "formats": [".md", ".csv", ".json"],
        "chunk_size": 256,  # 客服场景使用小块，更精准
        "chunk_overlap": 32,
        "faq_mode": True  # 优化 FAQ 格式的分块
    },
    "retrieval": {
        "top_k": 3,
        "similarity_threshold": 0.8,  # 更高的阈值确保准确性
        "intent_classification": True  # 先分类意图再检索
    },
    "generation": {
        "model": "gpt-3.5-turbo",  # 客服场景速度优先
        "temperature": 0.3,
        "system_prompt": """你是客服助手，帮助解答用户问题。

回答规范：
1. 用简洁、友好的语言
2. 提供具体的操作步骤
3. 控制在 200 字以内
4. 如果无法解决，引导联系人工客服

语气要求：
- 礼貌、耐心
- 避免使用技术术语"""
    }
}
```

### 场景 4：学术论文问答

```python
academic_config = {
    "name": "学术论文 RAG",
    "documents": {
        "formats": [".pdf"],
        "chunk_size": 800,  # 学术论文需要更大的上下文
        "chunk_overlap": 100,
        "extract_references": True,  # 提取参考文献
        "preserve_sections": True
    },
    "retrieval": {
        "top_k": 5,
        "similarity_threshold": 0.65,
        "citation_tracking": True  # 追踪引用关系
    },
    "generation": {
        "model": "gpt-4",
        "temperature": 0.2,
        "system_prompt": """你是学术研究助手，帮助理解和分析学术论文。

回答要求：
1. 准确理解学术概念和术语
2. 明确标注信息来源（论文标题、章节）
3. 区分事实陈述和推测
4. 提供相关的背景知识
5. 支持跨论文的对比分析

格式规范：
- 引用格式：[论文标题, 年份]
- 关键术语提供中英文对照"""
    }
}
```

### 场景配置对比

| 场景 | Chunk Size | Top-K | Temperature | 核心特点 |
|------|-----------|-------|-------------|---------|
| 企业知识库 | 512 | 5 | 0.3 | 权限控制、元数据丰富 |
| 技术文档 | 384 | 3 | 0.2 | 代码保留、精准引用 |
| 客服知识库 | 256 | 3 | 0.3 | 快速响应、FAQ 优化 |
| 学术论文 | 800 | 5 | 0.2 | 大上下文、引用追踪 |

## 扩展阅读

- LangChain RAG 教程
- LlamaIndex 文档
- ChromaDB 官方文档
- Hugging Face Embeddings

---

**项目难度：** ⭐⭐⭐ (中级)  
**预计用时：** 1-2 天  
**更新日期：** 2026-03-30
